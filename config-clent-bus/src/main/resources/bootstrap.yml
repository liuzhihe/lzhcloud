eureka:
  client:
    service-url:
      defaultZone: http://peer1:9001/eureka/

spring:
  cloud:
    config:
      name: config-client
      profile: dev
      label: master
      discovery:
        enabled: true
        service-id: CONFIG-SERVER-BUS